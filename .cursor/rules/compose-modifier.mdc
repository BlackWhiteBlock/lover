---
description: Compose Modifier 命名与传参约定，避免 modifier/Modifier 混用
globs: android/**/*.kt
alwaysApply: false
---

# Compose `modifier` 约定

Kotlin 大小写敏感。`Modifier`（参数名 / 局部变量）与 `Modifier`（Compose 类型/伴生对象）绝不能混用。

## 命名参数必须用小写 `modifier`

```kotlin
// ❌ BAD — 参数名误写成大写 Modifier（会导致 Box 等 content 重载匹配失败）
Box(Modifier = Modifier.fillMaxWidth()) { ... }

// ✅ GOOD — 参数名小写 modifier，右侧类型/工厂大写 Modifier
Box(modifier = Modifier.fillMaxWidth()) { ... }
```

## 传入值时写全 `modifier = Modifier.…`

```kotlin
// ❌ BAD — 当前 Composable 没有 modifier 参数时，裸写 modifier.xxx 会 Unresolved
// ❌ BAD — 或误写成 Modifier.weight 却漏了 modifier =（难读、易再错）
Column(Modifier.weight(1f)) { ... }

// ✅ GOOD — 默认写法：显式命名参数 + Modifier 工厂
Column(modifier = Modifier.weight(1f)) { ... }

// ✅ GOOD — 仅当函数声明了 modifier: Modifier 时，才链式用小写变量
@Composable
fun Foo(modifier: Modifier = Modifier) {
    Column(modifier = modifier.fillMaxWidth()) { ... }
}
```

## 自检（改完 Compose 布局必做）

1. 命名参数只能是小写：`modifier =`
2. 新建 Modifier 链用大写：`Modifier.…`
3. 小写 `modifier.xxx` 仅当本函数参数列表里已有 `modifier: Modifier`
