# 阿里云号码认证 Android SDK

请从 [号码认证控制台 → API & SDK](https://dypns.console.aliyun.com/) 下载 **Android** SDK，将解压后的 **3 个 aar** 全部放入本目录：

- `auth_*.aar`（主包）
- `logger_*.aar`
- `main_*.aar`

放入后 Sync Gradle，一键登录即可使用真实 SDK。未放入时 App 仍可编译，登录页仅展示短信登录。
