# Copy to keystore.properties and fill in real values.
# keystore.properties is gitignored — never commit passwords.

storeFile=lover-release.jks
storePassword=your-store-password
keyAlias=lover
keyPassword=your-key-password
